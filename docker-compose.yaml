services:
  adminer:
    image: adminer
    restart: always
    hostname: adminer
    networks:
      - internal
    ports:
      - 8080:8080

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: cloudflared
    restart: always
    networks:
      - internal
    command: tunnel --no-autoupdate run --token eyJhIjoiZWQ2NmE1NDZlYWVhMWNlMDBlYjE2YmI1Yzc1NmFkY2EiLCJ0IjoiNjRlN2ZmZjQtZTg1My00OWNjLTljMDgtM2JkOWRlMjk2ZTZlIiwicyI6IlpqY3pNamMyT1RndE1XSXhOUzAwT1RjeExXSmhNV1V0T1RBMFpXUmxZV05qTVdVMiJ9
  app:
    image: app
    hostname: app
    restart: always
    container_name: app
    build:
      context: ./
      dockerfile: Dockerfile
      # ADD THIS SECTION IF you use NEXT_PUBLIC variables in your frontend code
      args:
        NEXT_PUBLIC_MONGODB_URI: 'mongodb+srv://cachorro_cami:Tengo1amo@cluster0.ffbyjzl.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0'
    env_file: .env
    environment:
      PORT: 3000
      # These are perfect here (Runtime Secrets)
      MONGODB_URI: ${MONGODB_URI}
      DATABASE_URL: postgresql://postgres:Tengo1amo@postgres:5432/RyS?schema=public
      NODE_ENV: production
    ports:
      - 5555:3000
    expose:
      - 3000
    networks:
      - internal
  pgadmin:
    image: dpage/pgadmin4
    container_name: pgadmin
    restart: always
    environment:
      # These credentials are used to log into the pgAdmin UI
      PGADMIN_DEFAULT_EMAIL: ${PGADMIN_EMAIL:-juankpato87@gmail.com}
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-Tengo1amo}
    ports:
      - '5050:80'
    expose:
      - 80 # Access via localhost:5050
    networks:
      - internal
    volumes:
      - pgadmin-data:/var/lib/pgadmin

volumes:
  pgadmin-data:
networks:
  internal:
    external: true
    name: internal
